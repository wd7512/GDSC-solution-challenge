// Class storing accessibility preferences 

class SettingsStore{
  static bool isColorBlind = false;
  static double imageSize = 1.0;
  static double textSize = 16.0; // probably not needed as we will have images
}
